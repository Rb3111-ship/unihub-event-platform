{
    "ConnectionStrings": {
        // DB file will be created next to the executable
        "Sqlite": "Data Source=../notification.db"
    },

    "Smtp": {
        "Host": "sandbox.smtp.mailtrap.io",
        "Port": 587,
        "User": "15614d29c70958",
        //"User": "9feb6db72aa86d", //use your accountS username
        // "Pass": "****a2b0", // use your password
        "Pass": "9feb6db72aa86d",
        "From": "no-reply@unihub.local"
    },

    // shared secret every backend that calls the service must send in the header x-api-key
    "ApiKey": "DarkKnight123",

    // rate-limit: max 30 requests per minute per IP
    "IpRateLimiting": {
        "EnableEndpointRateLimiting": true,
        "StackBlockedRequests": false,
        "RealIpHeader": "X-Real-IP",
        "ClientIdHeader": "X-ClientId",
        "GeneralRules": [
            {
                "Endpoint": "*",
                "Period": "1m",
                "Limit": 30
            }
        ]
    },

    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*"
}
